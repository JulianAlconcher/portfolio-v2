---
import { projects, categories } from "../data/projects";
import { Github, ExternalLink, Search, Layers, Star, Sparkles, FolderGit2, AlertCircle } from "lucide-astro";
---

<section id="proyectos" class="relative py-20 sm:py-28 border-t border-zinc-800/80 bg-[#09090b]">
  <div class="relative mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    
    <!-- Section Header -->
    <div class="flex flex-col md:flex-row md:items-end justify-between gap-6 mb-10">
      <div>
        <p class="text-xs font-mono uppercase tracking-wider text-zinc-400 mb-2">
          Portfolio de Software
        </p>
        <h2 class="text-3xl sm:text-4xl font-bold tracking-tight text-white">
          Proyectos & Aplicaciones
        </h2>
        <p class="mt-2 text-sm sm:text-base text-zinc-400 max-w-2xl">
          Soluciones de agricultura satelital con Machine Learning, sitios web en producción, aplicaciones de sistemas en macOS y proyectos de código abierto.
        </p>
      </div>

      <!-- Quick External Link -->
      <a
        href="https://github.com/JulianAlconcher?tab=repositories"
        target="_blank"
        rel="noopener noreferrer"
        class="inline-flex items-center gap-2 rounded-xl border border-zinc-800 bg-zinc-900/60 px-4 py-2 text-xs font-medium text-zinc-300 hover:border-zinc-700 hover:bg-zinc-800 hover:text-white transition-all self-start md:self-auto"
      >
        <Github class="h-3.5 w-3.5" />
        <span>GitHub ({projects.length}+ repos)</span>
        <ExternalLink class="h-3 w-3 text-zinc-500" />
      </a>
    </div>

    <!-- Interactive Filter & Search Bar -->
    <div class="mb-10 space-y-4">
      <!-- Search Input -->
      <div class="relative max-w-md">
        <Search class="absolute left-3.5 top-1/2 -translate-y-1/2 h-4 w-4 text-zinc-500" />
        <input
          id="project-search"
          type="text"
          placeholder="Buscar por tecnología o nombre (ej: React, Swift, NDVI)..."
          class="w-full rounded-xl border border-zinc-800 bg-zinc-900/80 pl-10 pr-8 py-2.5 text-xs sm:text-sm text-zinc-100 placeholder-zinc-500 outline-none focus:border-zinc-600 focus:bg-zinc-900 transition-colors font-sans"
        />
        <button
          id="clear-search-btn"
          type="button"
          class="hidden absolute right-3 top-1/2 -translate-y-1/2 rounded text-xs text-zinc-500 hover:text-white"
          title="Limpiar"
        >
          ✕
        </button>
      </div>

      <!-- Category Filter Pills (Minimalist Segmented Control) -->
      <div class="flex flex-wrap items-center gap-1.5 p-1 rounded-xl bg-zinc-900/80 border border-zinc-800/80 w-fit">
        {categories.map((cat) => (
          <button
            type="button"
            class="filter-pill rounded-lg px-3 py-1.5 text-xs font-medium text-zinc-400 hover:text-zinc-200 transition-all cursor-pointer flex items-center gap-1.5 data-[active=true]:bg-zinc-800 data-[active=true]:text-white data-[active=true]:shadow-sm"
            data-category={cat.id}
            data-active={cat.id === "all" ? "true" : "false"}
          >
            <span>{cat.label}</span>
            <span class="text-[10px] text-zinc-500 font-mono data-[active=true]:text-zinc-400">
              {cat.count}
            </span>
          </button>
        ))}
      </div>

      <!-- Live Counter -->
      <div class="flex items-center justify-between text-xs text-zinc-500 pt-0.5 font-mono">
        <span id="results-count">Mostrando {projects.length} de {projects.length} proyectos</span>
        <button id="reset-filters" class="hidden text-zinc-400 hover:text-white underline">Restablecer</button>
      </div>
    </div>

    <!-- Projects Grid -->
    <div id="projects-grid" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      {projects.map((project) => (
        <article
          class="project-card group relative flex flex-col overflow-hidden rounded-2xl border border-zinc-800/80 bg-[#111114] transition-all duration-200 hover:-translate-y-1 hover:border-zinc-700 hover:shadow-xl"
          data-category={project.category}
          data-featured={project.featured ? "true" : "false"}
          data-tech={project.tech.join(" ").toLowerCase()}
          data-title={project.title.toLowerCase()}
          data-description={project.description.toLowerCase()}
        >
          <!-- Project Thumbnail Image / Header -->
          <div class="relative h-44 w-full overflow-hidden bg-zinc-900 border-b border-zinc-800">
            <img
              src={project.image}
              alt={project.title}
              loading="lazy"
              class="h-full w-full object-cover transition-transform duration-500 group-hover:scale-105"
            />
            <div class="absolute inset-0 bg-linear-to-t from-[#111114] via-[#111114]/30 to-transparent"></div>
            
            <!-- Category Badge Pill -->
            <div class="absolute top-3 left-3 z-10">
              <span class="inline-flex items-center gap-1.5 rounded-lg border border-zinc-700/60 bg-zinc-900/90 px-2.5 py-1 text-[11px] font-medium text-zinc-200 backdrop-blur-md">
                {project.featured && <span class="h-1.5 w-1.5 rounded-full bg-blue-400"></span>}
                {project.badge}
              </span>
            </div>

            <!-- Stars count if available -->
            {project.stars > 0 && (
              <div class="absolute top-3 right-3 z-10">
                <span class="inline-flex items-center gap-1 rounded-lg border border-amber-500/30 bg-amber-500/15 px-2 py-0.5 text-[11px] font-medium text-amber-300 backdrop-blur-md">
                  <Star class="h-3 w-3 fill-amber-400" />
                  {project.stars}
                </span>
              </div>
            )}

            <!-- Title on image bottom -->
            <div class="absolute bottom-3 left-4 right-4 z-10">
              <h3 class="text-lg font-bold text-white group-hover:text-zinc-200 transition-colors">
                {project.title}
              </h3>
            </div>
          </div>

          <!-- Project Card Content -->
          <div class="flex flex-1 flex-col p-5 justify-between">
            
            <!-- Top Section: Description & Challenge -->
            <div class="space-y-3 flex flex-col">
              <!-- Description with uniform height -->
              <p class="text-xs sm:text-sm text-zinc-400 leading-relaxed line-clamp-3 min-h-[4rem]">
                {project.description}
              </p>

              <!-- Challenge / Note Box with uniform fixed height & alignment -->
              <div class="rounded-xl border border-zinc-800 bg-zinc-900/50 p-3 text-xs min-h-[4.75rem] flex flex-col justify-center">
                <span class="font-mono text-[10px] uppercase text-zinc-500 block mb-0.5 font-semibold">Desafío</span>
                <span class="text-zinc-300 line-clamp-2 leading-relaxed">{project.challenge}</span>
              </div>
            </div>

            <!-- Bottom Section: Tech Badges & Links pinned to bottom -->
            <div class="mt-4 pt-1 space-y-3.5">
              <!-- Tech Badges with uniform height -->
              <div class="flex flex-wrap content-start gap-1.5 min-h-[3.75rem] max-h-[4.25rem] overflow-hidden">
                {project.tech.map((t) => (
                  <span class="rounded-md border border-zinc-800 bg-zinc-900/80 px-2 py-0.5 text-[11px] font-mono text-zinc-400">
                    {t}
                  </span>
                ))}
              </div>

              <!-- Action Links / Footer -->
              <div class="flex items-center justify-between pt-3 border-t border-zinc-800 text-xs">
                <span class="inline-flex items-center gap-1.5 text-zinc-500 text-[11px] truncate max-w-[140px]" title={project.role}>
                  <Layers class="h-3 w-3 text-zinc-400 shrink-0" />
                  <span class="truncate">{project.role}</span>
                </span>

                <div class="flex items-center gap-2 shrink-0">
                  {project.github && (
                    <a
                      href={project.github}
                      target="_blank"
                      rel="noopener noreferrer"
                      class="inline-flex items-center gap-1.5 rounded-lg border border-zinc-700/80 bg-zinc-800/80 px-2.5 py-1 text-xs font-medium text-zinc-200 hover:bg-zinc-700 hover:text-white transition-colors"
                      title="Ver Código"
                    >
                      <Github class="h-3.5 w-3.5" />
                      <span>Repo</span>
                    </a>
                  )}

                  {project.demo && (
                    <a
                      href={project.demo}
                      target="_blank"
                      rel="noopener noreferrer"
                      class="inline-flex items-center gap-1.5 rounded-lg bg-white px-2.5 py-1 text-xs font-semibold text-zinc-900 hover:bg-zinc-200 transition-colors"
                      title="Ver Sitio en Vivo"
                    >
                      <ExternalLink class="h-3 w-3" />
                      <span>Live</span>
                    </a>
                  )}
                </div>
              </div>
            </div>

          </div>
        </article>
      ))}
    </div>

    <!-- Empty Results State -->
    <div id="no-results" class="hidden text-center py-16 px-4">
      <div class="mx-auto flex h-12 w-12 items-center justify-center rounded-xl bg-zinc-900 border border-zinc-800 text-zinc-500 mb-3">
        <AlertCircle class="h-6 w-6" />
      </div>
      <h3 class="text-sm font-semibold text-white mb-1">No se encontraron proyectos</h3>
      <p class="text-xs text-zinc-500 max-w-sm mx-auto mb-4">
        Prueba buscando con otro término o seleccionando "Todos".
      </p>
      <button
        id="empty-reset-btn"
        type="button"
        class="inline-flex items-center gap-2 rounded-lg bg-zinc-800 px-3.5 py-1.5 text-xs font-medium text-white hover:bg-zinc-700 transition-colors"
      >
        Limpiar Filtros
      </button>
    </div>

  </div>
</section>

<script>
  let activeCategory = "all";
  let searchQuery = "";

  const searchInput = document.getElementById("project-search") as HTMLInputElement;
  const clearSearchBtn = document.getElementById("clear-search-btn") as HTMLButtonElement;
  const filterPills = document.querySelectorAll(".filter-pill");
  const projectCards = document.querySelectorAll(".project-card") as NodeListOf<HTMLElement>;
  const resultsCount = document.getElementById("results-count") as HTMLElement;
  const resetFiltersBtn = document.getElementById("reset-filters") as HTMLButtonElement;
  const emptyResetBtn = document.getElementById("empty-reset-btn") as HTMLButtonElement;
  const noResults = document.getElementById("no-results") as HTMLElement;
  const totalCount = projectCards.length;

  function applyFilters() {
    let visibleCount = 0;
    const q = searchQuery.toLowerCase().trim();

    projectCards.forEach((card) => {
      const cat = card.getAttribute("data-category") || "";
      const isFeatured = card.getAttribute("data-featured") === "true";
      const title = card.getAttribute("data-title") || "";
      const desc = card.getAttribute("data-description") || "";
      const tech = card.getAttribute("data-tech") || "";

      let categoryMatch = false;
      if (activeCategory === "all") {
        categoryMatch = true;
      } else if (activeCategory === "featured") {
        categoryMatch = isFeatured;
      } else if (activeCategory === "tools") {
        categoryMatch = cat === "tools" || cat === "bots";
      } else {
        categoryMatch = cat === activeCategory;
      }

      let searchMatch = true;
      if (q) {
        searchMatch = title.includes(q) || desc.includes(q) || tech.includes(q);
      }

      if (categoryMatch && searchMatch) {
        card.style.display = "flex";
        visibleCount++;
      } else {
        card.style.display = "none";
      }
    });

    if (resultsCount) {
      resultsCount.textContent = `Mostrando ${visibleCount} de ${totalCount} proyectos`;
    }

    if (visibleCount === 0) {
      noResults?.classList.remove("hidden");
    } else {
      noResults?.classList.add("hidden");
    }

    const isFiltered = activeCategory !== "all" || q.length > 0;
    if (resetFiltersBtn) {
      if (isFiltered) {
        resetFiltersBtn.classList.remove("hidden");
      } else {
        resetFiltersBtn.classList.add("hidden");
      }
    }
  }

  filterPills.forEach((pill) => {
    pill.addEventListener("click", () => {
      filterPills.forEach((p) => {
        p.setAttribute("data-active", "false");
      });
      pill.setAttribute("data-active", "true");
      activeCategory = pill.getAttribute("data-category") || "all";
      applyFilters();
    });
  });

  searchInput?.addEventListener("input", (e) => {
    searchQuery = (e.target as HTMLInputElement).value;
    if (searchQuery) {
      clearSearchBtn?.classList.remove("hidden");
    } else {
      clearSearchBtn?.classList.add("hidden");
    }
    applyFilters();
  });

  function resetAll() {
    activeCategory = "all";
    searchQuery = "";
    if (searchInput) searchInput.value = "";
    clearSearchBtn?.classList.add("hidden");

    filterPills.forEach((p) => {
      p.setAttribute("data-active", p.getAttribute("data-category") === "all" ? "true" : "false");
    });
    applyFilters();
  }

  clearSearchBtn?.addEventListener("click", () => {
    searchQuery = "";
    if (searchInput) {
      searchInput.value = "";
      searchInput.focus();
    }
    clearSearchBtn.classList.add("hidden");
    applyFilters();
  });

  resetFiltersBtn?.addEventListener("click", resetAll);
  emptyResetBtn?.addEventListener("click", resetAll);
</script>
